"use client";
import { Reveal, Pill } from "./magical/MagicMotion";
export function RealEstateListings({title,items}:{title:string;items:{name:string;location:string;price:string;imageUrl:string}[]}) {
 return <section id="properties" className="bg-white px-6 py-28 lg:px-10"><div className="mx-auto max-w-7xl"><Reveal><Pill>Selected properties</Pill><h2 className="mt-6 max-w-3xl text-5xl font-semibold tracking-[-.05em]">{title}</h2></Reveal><div className="mt-14 grid gap-5 md:grid-cols-2">{items.map((x,i)=><Reveal key={x.name} delay={i*100}><article className={`group relative overflow-hidden rounded-[2rem] ${i===0?"md:row-span-2":""}`}><div className={i===0?"aspect-[4/5]":"aspect-[16/10]"}><img src={x.imageUrl} alt={x.name} className="h-full w-full object-cover transition duration-1000 group-hover:scale-105"/></div><div className="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent"/><div className="absolute inset-x-6 bottom-6 text-white"><p className="text-xs uppercase tracking-widest text-white/60">{x.location}</p><div className="mt-2 flex items-end justify-between gap-4"><h3 className="text-2xl font-semibold">{x.name}</h3><span>{x.price}</span></div></div></article></Reveal>)}</div></div></section>;
}
